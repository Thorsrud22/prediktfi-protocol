import { describe, it, expect } from 'vitest';

describe('copy-button placeholder', () => {
  it('should have a test placeholder to satisfy Vitest runner', () => {
    expect(true).toBe(true);
  });
});
